import PrivatePage from './private.page';

describe('PrivatePage', () => {
  let component: PrivatePage;

  beforeEach(() => {
    component = new PrivatePage();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
